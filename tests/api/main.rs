/// Link once and run many
/// We are consolidating into a single binary integration crate
/// vs. having each mod be a sep. binary.
mod health_check;
mod helpers;
mod subscriptions;
mod subscriptions_confirm;
